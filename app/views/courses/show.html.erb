<p id="notice"><%= notice %></p>

<%# 
Computer science course is the need for the students now a days, as we all knew that everything is now managed using computers.
Each and Every thing is computerized now a days and we should learn how to build such a nice things by using the computers.
Thank You  %>



<div class="row">
  <div class="col s12 m12">
    <div class="card blue-grey darken-1">
      <div class="card-content white-text">
        <span class="card-title">
          <%= @course.short_name %> - <%= @course.name %>
        </span>
        <p><%= @course.description %></p>
      </div>
      <div class="card-action">
        <% if !current_student.courses.include?(@course) %>
          <%= link_to "Enroll", enroll_course_path(course_id: @course.id), method: :post %>
        <% else %>
          <span class="green white-text">Enrolled</span>
        <% end %>
        <%= link_to 'Edit', edit_course_path(@course) %>
        <%= link_to 'Back', courses_path %>
      </div>
    </div>
  </div>
</div>



